// file should export all constant values repeatedly used across the tests
export const TEST_USER_EMAIL = (browser, domain) => `e2e_test_${browser}@${domain}`;
export const TEST_USER_PASSWORD = 'PlaywrightTest12345!';